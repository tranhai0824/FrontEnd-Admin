{
  "name": "@scholarship/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "echo 'no dev task'",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@scholarship/config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
